[build]
  command = "npm run build"
  publish = "dist"

# SPA : toutes les routes React Router (/dashboard, /crypto/bitcoin, ...) servent index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
